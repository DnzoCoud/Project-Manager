/**
 * Esto es una simulacion de un .env, logicamente en un entorno de produccion no se usaran estas constantes si no un env definido
 */
export const API_CONSTANTS = {
  BASE_URL: "http://localhost:8000/api/v1",
  LOCAL_TOKEN_NAME: "project_manage_token",
};
